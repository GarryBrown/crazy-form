import { HostContainerDirective } from './host-container.directive';

describe('HostContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new HostContainerDirective();
    expect(directive).toBeTruthy();
  });
});
